---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: FLUX_REPO_NAME
  namespace: flux-system
spec:
  interval: 18m
  sourceRef:
    kind: GitRepository
    name: FLUX_REPO_NAME
    namespace: flux-system
  git:
    checkout:
      ref:
        branch: master
    commit:
      author:
        email: FLUX_AUTOMATION_EMAIL
        name: Flux2
      messageTemplate: '{{range .Updated.Images}}{{println .}}{{end}}'
    push:
      branch: master
  update:
    path: ./clusters/PLACEHOLDER
    strategy: Setters
